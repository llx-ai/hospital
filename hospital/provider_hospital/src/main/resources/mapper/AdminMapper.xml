<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etc.hospital.fl.dao.AdminDao">


  <select id="loginManager" parameterType="admin" resultType="admin">
    select * from h_admin where admin_name = #{admin_name} and admin_pwd =#{admin_pwd}
  </select>
</mapper>